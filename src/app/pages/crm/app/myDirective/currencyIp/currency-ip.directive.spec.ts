import { CurrencyIpDirective } from './currency-ip.directive';

describe('CurrencyIpDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyIpDirective();
    expect(directive).toBeTruthy();
  });
});
